<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/detailed/movies-series.css">
    <title>FIAP Movies - <%= serie.name ? serie.name : serie.original_name %> </title>
</head>
<body>
    <%- include('../templates/header') %> 
    <main>
        <div class="background" style="background: url('https://image.tmdb.org/t/p/original/<%= serie.backdrop_path %>') #000;">
            <div class="movie-informations">
                <figure>
                    <img src="https://image.tmdb.org/t/p/w500/<%= serie.poster_path %>" alt="Capa da série '<%= serie.name ? serie.name : serie.original_name %>' ">
                </figure>
                <div class="movie-title-description">
                    <h2>
                        <%= serie.name ? serie.name : serie.original_name %> 
                    </h2>
                    <p>
                        <%= serie.overview %> 
                    </p>
                </div>
            </div>
        </div>
        <div class="section-1">
            <div class="section-left">
                <div class="buttons">
                    <button><a id="button-icon-1" href="/">Voltar</a></button>
                    <button><a id="button-icon-2" href="http://www.google.com/search?q=Site+do+filme+<%= serie.name ? serie.name.replace(" ", "+") : serie.original_name%>" target="_blank" rel="noopener">Site</a></button>
                    <button><a href="https://www.youtube.com/results?search_query=<%= serie.name ? serie.name.replace(" ", "+") : serie.original_name %>+trailer" target="_blank" rel="noopener">Trailer</a></button>
                </div>
                <div class="casting">
                    <h3>Elenco</h3>
                    <div class="cast-members">
                        <% for (let i = 0; i < 10; i++) { %>
                            <% if (!casting.cast[i]) break %> 
                            <div class="card">
                                <div class="card-content-img">
                                    <figure>
                                        <img src="<%= casting.cast[i].profile_path ? `https://image.tmdb.org/t/p/w500/${casting.cast[i].profile_path}` : '/images/noprofilepicture.jpg' %> " alt="Foto do ator(a) '<%= casting.cast[i].name %> '">
                                        <figcaption> <%= casting.cast[i].name %> </figcaption>
                                    </figure>
                                </div>
                                <div class="card-content-txt">
                                    <p> <%= casting.cast[i].character %> </p>
                                </div>
                            </div>
                        <% } %> 
                    </div>
                </div>
            </div>
            <aside class="section-right">
                <h3>Ficha técnica</h3>
                <div class="detailed-informations">
                    <h4>Título original</h4>
                    <p><%= serie.original_name %> </p>
                </div>
                <div class="detailed-informations">
                    <h4>Idioma original</h4>
                    <p><%= serie.original_language.toUpperCase() %> </p>
                </div>
                <div class="detailed-informations">
                    <h4>Número de temporadas</h4>
                    <p><%= serie.number_of_seasons %> </p>
                </div>
                <div class="detailed-informations">
                    <h4>Criado por</h4>
                    <p><%= serie.created_by[0] ? serie.created_by[0].name : 'Unknown' %> </p>
                </div>
                <div class="detailed-informations">
                    <h4>Lançamento</h4>
                    <p><%= serie.first_air_date.split("-").reverse().join("/")  %> </p>
                </div>
                <div class="detailed-informations">
                    <h4>Nota</h4>
                    <p><%= serie.vote_average.toFixed(1) %> </p>
                </div>
            </aside>
        </div>
        <%- include('../templates/footer') %> 
    </main>
</body>
</html>
